import { Component } from '@angular/core';
import { Producto } from '../../models/Producto';

@Component({
  selector: 'app-reserva',
  standalone: false,
  templateUrl: './reserva.component.html',
  styleUrl: './reserva.component.css'
})
export class ReservaComponent {
 /* 
  productoSeleccionado!: Producto;

  sucursales: string[] = ['Bariloche snow ski', 'Malargue Mendoza', 'Mendoza capital','San rafael Mendoza'];

  fechaInicio!: string;
  fechaFin!: string;
  sucursalRetiro!: string;
  sucursalDevolucion!: string;
  total!: number;

  ngOnInit(): void {
    // Simulación: producto fijo hasta que conectemos con navegación real
    this.productoSeleccionado = {
      id: 1,
      nombre: 'Campera impermeable',
      imagenUrl: 'assets/img/campera.jpg'
    };
  } 

  

  confirmarReserva(): void {
    console.log('Reserva confirmada:', {
      producto: this.productoSeleccionado,
      fechaInicio: this.fechaInicio,
      fechaFin: this.fechaFin,
      retiro: this.sucursalRetiro,
      devolucion: this.sucursalDevolucion,
      total: this.total
    });
  }
*/

}
